'use strict';

describe('recruitApp.RegistrationController module', function() {

  beforeEach(module('recruitApp.RegistrationController'));

  describe('RegistrationController', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var registrationController = $controller('RegistrationController');
      expect(registrationController).toBeDefined();
    }));

  });
});